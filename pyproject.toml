[project]
name = "dbt-activities"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "dbt-bigquery>=1.10.2",
    "dbt-core>=1.10.11",
]

[dependency-groups]
dev = [
    "synthesia>=0.2.1",
]
